name: prana_mobile
description: PRANA ELEX station control and live translation for Android.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.0.0
  dio: ^5.10.0
  firebase_auth: ^6.0.0
  firebase_core: ^4.0.0
  flutter_riverpod: ^3.3.2
  flutter_secure_storage: ^9.2.4
  go_router: ^17.3.0
  google_sign_in: ^7.2.0
  intl: ^0.20.2
  mobile_scanner: ^7.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
